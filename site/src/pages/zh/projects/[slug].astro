---
import data from "../../../generated/data.json";
import ProjectPage from "../../../components/ProjectPage.astro";
export function getStaticPaths() { return data.projects.map((project) => ({ params: { slug: project.id }, props: { project } })); }
const { project } = Astro.props;
---
<ProjectPage project={project} lang="zh" />
